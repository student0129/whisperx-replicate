# --------------------
# File: cog.yaml
# --------------------
# Replicate build + prediction configuration

build:
  gpu: true
  python_version: "3.10"
  system_packages:
    - ffmpeg
  python_requirements: requirements.txt

predict: "predict.py:Predictor"
